### Export Image of canvas

We need export image of canvas. it can be png format , svg format or copy to clipboard

Requirements:

- when export button it opens a modal with preview of image and three button at bottom to export image in png,svg and copy to clipboard
- if canvas is empty show error like can not export empty canvas

### Check when done

- new components compile without typescript errors
- no lint errors
- import and export must work
- use pnpm
